# CodeBanana control API
# WARNING: disabling auth makes the control API public to anyone who can reach the port.
DC_API_PORT=9102
DC_API_AUTH_REQUIRED=false
DC_API_HOST=0.0.0.0
# If auth is enabled, set a long random token:
# DC_API_TOKEN=replace-with-a-long-random-token
