# Control API secret - set in the CodeBanana runtime, never commit the real value.
DC_API_TOKEN=replace-with-a-long-random-token
# Use the port allocated by CodeBanana. 8000 is the documented example.
DC_API_PORT=8000
